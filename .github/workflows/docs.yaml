name: Deploy S2DM Docs to GitHub Pages
# Live site: https://covesa.github.io/s2dm-vocab/

on:
  push:
    branches: [main]
    paths:
      - "spec/**"
      - ".github/workflows/docs.yaml"
      - "pyproject.toml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  docs:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.docs.outputs.page-url }}

    steps:
      - uses: actions/checkout@v4

      - id: docs
        uses: COVESA/s2dm/actions/s2dm-docs-build@main
        with:
          project-title: "S2DM Vocabulary"
          project-name: s2dm-vocab
          org-name: COVESA
          pages-url: https://covesa.github.io/s2dm-vocab
          github-repo-url: https://github.com/COVESA/s2dm-vocab
          schema-sources: "spec"
